<?xml version="1.0" encoding="utf-8"?>

<!-- TODO(13)DONE: Definir un LinearLayout, con ancho y altura de toda la pantalla, centrado, con un margen interior de 12 dp, vertical y que muestre "StartActivity"-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="12dp"
    android:background="@color/colorWhite"
    android:gravity="center"
    android:orientation="vertical"
    android:tooltipText="StartActivity">
    <!-- TODO(14)DONE: Mostrar el logo, con 50 píxeles de altura -->
    <ImageView
        android:id="@+id/imageView2"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        app:srcCompat="@drawable/logo" />
    <!-- TODO(15)DONE: Crear un Button con texto de logarse, cuya anchura sea toda la disponible, con altura ajustable, un margen superior de 15dp, color de fuent3e blanco y color de fondo drawable/button_background -->
    <Button
        android:id="@+id/button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:background="@drawable/button_background"
        android:text="Logarse"
        android:textColor="@color/colorWhite" />

    <!-- TODO(16)DONE: Mismo componente que TODO 15 con texto registrar -->
    <Button
        android:id="@+id/button2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:background="@drawable/button_background"
        android:text="Registrar"
        android:textColor="@color/colorWhite" />
</LinearLayout>

